#!/bin/sh
# ROS2 entry — LeRobot is installed in /opt/venv (see install_lerobot.sh).
PY=/opt/venv/bin/python3
if [ ! -x "$PY" ]; then
  PY=/usr/bin/python3
fi
exec "$PY" -m autonomy_lerobot.dataset_viz "$@"
