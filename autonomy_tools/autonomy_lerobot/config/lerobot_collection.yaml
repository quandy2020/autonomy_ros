# LeRobot bridge for multi-robot collection (RGB + semantic only).
# Map/plan/costmap are single-channel grids; skip them to avoid PNG write errors.
lerobot_bridge_node:
  ros__parameters:
    rgb_topic: camera/rgb/image_raw
    depth_topic: camera/depth/image_raw
    semantic_topic: camera/semantic/image_raw
    camera_info_topic: camera/rgb/camera_info
    odom_topic: odom
    agent_pose_topic: habitat/agent_pose
    cmd_vel_topic: cmd_vel
    map_topic: map
    pointcloud_topic: semantic_pointcloud

    global_plan_topic: plan
    local_plan_topic: transformed_global_plan
    global_costmap_topic: global_costmap/costmap
    local_costmap_topic: local_costmap/costmap

    use_depth: false
    record_semantic: true
    record_map: false
    record_pointcloud: false
    record_camera_info: true
    record_nav2: false

    image_width: 640
    image_height: 480
    max_path_waypoints: 512
    max_pointcloud_points: 4096

    dataset_repo_id: local/habitat_collection
    record_fps: 10.0
    task: navigate to goal

    video_vcodec: h264
    streaming_encoding: true
    parallel_video_encoding: true
