# 请求：无
string request_id
---
# 响应：高度信息
# true, false
float32 cur_height_re   # 当前相对高度（cm），[-5， 5]
float32 cur_height_ab   # 当前绝对高度（cm），[0， 10]
float32 cur_height_pct  # 当前高度百分比%，[0， 100]
bool success
string message